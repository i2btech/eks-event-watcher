apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eew-chart.fullname" . }}-test-connection"
  labels:
    {{- include "eew-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eew-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
